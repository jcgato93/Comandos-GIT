git stash: es otro de los limbos, como 
el staging area que se establece para cada rama de forma individual. 
Para agregar los cambios estos deben estar en el staging area.

git stash list: nos muestra la lista de stash que tengamos.

git stash drop stash@{numero}: nos permite borrar un stash.

git stash apply: aplicamos el último cambio

git stash apply stash@{numero}: aplicamos del stash especifico
